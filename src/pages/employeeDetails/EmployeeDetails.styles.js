import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
  container: {
    background: "#F3F9F9 0% 0% no-repeat padding-box",
    opacity: 1,
  },
}));

export default useStyles;
