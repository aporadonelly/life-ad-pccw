import { makeStyles } from "@material-ui/styles";

const styles = makeStyles((theme) => ({
  viewBtn: {
    margin: "0 5px",
    background: "#EF841F",
    color: "#fff",
    cursor: "pointer",
  },
}));

export default styles;
